<?xml version="1.0" encoding="utf-8"?>
<!--Navigation drawer layout-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dl_main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    android:scrollbars="none">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/fl_activity_main_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <!-- The navigation drawer List-->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nd_activity_main_navigator"
        android:layout_width="@dimen/dimen_250dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:choiceMode="singleChoice"
        app:headerLayout="@layout/activity_main_drawer_header"
        app:itemIconTint="@drawable/bg_drawer_menu_item"
        app:itemTextColor="@drawable/bg_drawer_menu_item"
        app:menu="@menu/drawer_item_list"
        android:gravity="center_vertical|start"
        android:theme="@style/NavigationTheme" />

</androidx.drawerlayout.widget.DrawerLayout>


